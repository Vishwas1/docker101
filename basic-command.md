## Images

```sh
docker images --help
docker pull jboss/keycloak
docker images ## to list all images
docker images -q ## to list all images ids.
docker images -f "dangling=false"  ## filter images
docker run jboss/keycloak # running the container - an instance of image
```

